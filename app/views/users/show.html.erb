<div class="container">  <!-- この行を追加 -->
  <div class="row">      <!-- この行を追加 -->
    <div class="col-md-3">
    <%= render 'user', user: @users %>
    <%= render 'books/book', book: @book %>
    </div>
    <div class="col-md-8 offset-md-1">
      <h1>books</h1>
    <table class="table table-hover table-inverse">
    <thead>
    <tr>
    <th></th>
    <th>Title</th>
    <th>Opinion</th>
    <th></th>
    </tr>
  </thead>
    <tr>

      <% @books.each do |book| %>


      <td>
        <%= link_to user_path(@users) do %>
        <%= image_tag @users. get_image %>
      <% end %></td>
      <td><%= link_to book_path(book) do %>
      <%= book.title %>
      <% end %></td>
      <td><%= book.body %></td>
      <td></td>
    </tr>
      <% end %>


      </table>
    </div>
  </div>
</div>