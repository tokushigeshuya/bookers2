<div class="container">  <!-- この行を追加 -->
  <div class="row">      <!-- この行を追加 -->
    <div class="col-lg-3">
    <%= render 'user', user: @user %>
    <%= render 'books/book', book: @book %>
    </div>
    <div class="col-lg-9">
      <h1>books</h1>
    <table class="table">
    <thead>
    <tr>
    <th></th>
    <th>Title</th>
    <th></th>
    <th>Opinion</th>
    </tr>
  </thead>
    <tr>
      <% @books.each do |book| %>
      <td><%= image_tag @users. get_image %></td>
      <td><%= book.body %></td>
      <td><%= link_to 'Show', book_path(book) %></td>

    </tr>
      <% end %>


      </table>
    </div>
  </div>
</div>